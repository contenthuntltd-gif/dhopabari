# ============================================================
# Dhopa Bari — nginx site config (Contabo VPS)
#
# Serves the built Flutter web app as static files. All backend work
# (database, auth, edge functions) happens in Supabase cloud, so this
# server only needs to hand out the compiled front-end.
#
# Install:
#   sudo cp deploy/nginx-dhopabari.conf /etc/nginx/sites-available/dhopabari
#   sudo ln -s /etc/nginx/sites-available/dhopabari /etc/nginx/sites-enabled/
#   sudo nginx -t && sudo systemctl reload nginx
#
# Then run certbot (see DEPLOY.md) to add HTTPS — it rewrites this file.
# ============================================================

server {
    listen 80;
    listen [::]:80;

    # ↓↓↓ change to your real domain (or the VPS IP for a first test) ↓↓↓
    server_name dhopabari.com www.dhopabari.com;

    # Where the built app lives (see DEPLOY.md — copy build/web here).
    root /var/www/dhopabari;
    index index.html;

    # Flutter web is a single-page app: every unknown path must fall back
    # to index.html so in-app navigation and refresh-on-a-deep-link work.
    location / {
        try_files $uri $uri/ /index.html;
    }

    # Long-cache the fingerprinted assets, but never cache the shell files
    # so a new deploy is picked up immediately.
    location ~* \.(?:js|css|png|jpg|jpeg|gif|svg|woff2?|ttf|otf|wasm|json)$ {
        expires 7d;
        add_header Cache-Control "public";
    }
    location = /index.html {
        add_header Cache-Control "no-cache";
    }
    location = /flutter_service_worker.js {
        add_header Cache-Control "no-cache";
    }

    # gzip for the big main.dart.js
    gzip on;
    gzip_types text/plain text/css application/javascript application/json image/svg+xml application/wasm;
    gzip_min_length 1024;

    client_max_body_size 10m;
}
